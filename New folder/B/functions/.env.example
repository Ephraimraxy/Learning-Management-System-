# Copy to .env and fill
DAILY_API_KEY=your_daily_api_key_here   # Daily API key with permission to create rooms & meeting tokens
# Optionally set default meeting properties
DAILY_DEFAULT_ROOM_CONFIG='{"name":"default-room"}'
# Firebase env is configured via firebase CLI; for local function testing you can set PORT and other envs.
